---
sidebar_position: 8
---

import MystrixVisualizer from "@site/src/components/MystrixVisualizer/MystrixVisualizer";

# Bottom Controls

Row 23 (the bottom row) contains advanced features for external hardware control, collaboration, and session management.

## Control Layout Overview

export function BottomRowOverview() {
    const uiElements = [
        {
            "name": "WLED Controls",
            "desc": "Columns 0-3. Control external LED strips via WLED. The band drifts slowly, so the exact colours shown here are one moment of that cycle.",
            "elements": [
                { "pos": [0, 23], "size": [4, 1], "color": ["#CCC929", "#AFCC29", "#8ECC29", "#6DCC29"] }
            ]
        },
        {
            "name": "Link Button",
            "desc": "Columns 5-7. Ableton Link is built into Pixelboop but switched off in the current release, so those three pixels stay dark.",
            "elements": [
                { "pos": [5, 23], "size": [3, 1], "color": "#000000" }
            ]
        },
        {
            "name": "Mode Button",
            "desc": "Columns 8-9. Orchestra sync mode indicator (Yellow=Free, Purple=Follower, Bright Purple=Host, Bright Green=Remote).",
            "elements": [
                { "pos": [8, 23], "size": [2, 1], "color": "#C89600" }
            ]
        },
        {
            "name": "Sync Button",
            "desc": "Columns 10-12. Multi-device sync controls.",
            "elements": [
                { "pos": [10, 23], "size": [3, 1], "color": "#004D66" }
            ]
        },
        {
            "name": "Device Indicators",
            "desc": "Columns 12-35, one pixel per participant, up to 24. The host is bright green #00FF00, followers dimmer #00B300. Nothing is drawn at all when there are no participants.",
            "elements": [
                { "pos": [12, 23], "size": [1, 1], "color": "#004D66" },
                { "pos": [13, 23], "size": [1, 1], "color": "#00B300" },
                { "pos": [14, 23], "size": [1, 1], "color": "#00FF00" }
            ]
        }
    ];

    return <MystrixVisualizer
        uiName="Bottom Control Row"
        uiDescription="Row 23 contains WLED, Link, Mode, Sync, and device indicators"
        uiElements={uiElements}
    />
}

<BottomRowOverview />

---

## WLED Controls (Cols 0-3)

export function WLEDDemo() {
    const uiElements = [
        {
            "name": "WLED Gradient",
            "desc": "A slow-drifting colour band shows WLED output is on. Tap opens the Rainbow Menu, hold toggles output.",
            "elements": [
                { "pos": [0, 23], "size": [4, 1], "color": ["#CC2929", "#CC4229", "#CC5C29", "#CC7529"] }
            ]
        }
    ];

    return <MystrixVisualizer
        uiName="WLED Controls"
        uiDescription="Columns 0-3: Control external LED strips"
        uiElements={uiElements}
    />
}

<WLEDDemo />

### What is WLED?
WLED is an open-source LED strip control system. Pixelboop can send pattern data to WLED devices to create synchronized light shows.

### Visual Indicator
- **Slow-drifting colour band**: WLED output is ON (about one full cycle every ten seconds). It shows output is enabled, not that a device is reachable.
- **Dim orange (#281400)**: WLED output is off.

### Tap: Open the Rainbow Menu
- Opens the WLED configuration dialog (a "Rainbow Menu" tooltip confirms it)
- Configure the WLED IP address, brightness, and color mapping, and test the connection

### Hold: Toggle WLED On/Off
- Enables/disables WLED output
- When enabled, sends pattern data to the configured WLED device

### Features
- **Real-time sync**: LED patterns match sequencer playback
- **Pattern visualization**: Notes light up as they play
- **Guide lights**: Show beat positions when master sync enabled
- **Color mapping**: Tracks map to different LED colors

**See [WLED Setup Guide](../Connectivity/WLED.mdx) for full setup and configuration details.**

---

## Ableton Link (Cols 5-7)

:::note
**Ableton Link** is built into Pixelboop but switched off in the current release, so those three pixels stay dark.
:::

**See [Multi-Device Sync](../JamWithOthers/MultiDeviceSync.mdx) for collaboration setup.**

---

## Mode Button (Cols 8-9)

export function ModeButtonDemo() {
    const uiElements = [
        {
            "name": "Mode Button (Orchestra Indicator)",
            "desc": "Columns 8-9, Row 23. Color indicates Orchestra sync state.",
            "elements": [
                { "pos": [8, 23], "size": [2, 1], "color": "#C89600" }
            ]
        },
        {
            "name": "Remote session active",
            "desc": "With a remote session running the Mode pixels go bright green (#00E600), overriding the sync-mode colour below.",
            "elements": [
                { "pos": [8, 22], "size": [2, 1], "color": "#00E600" }
            ]
        },
        {
            "name": "Orchestra Follower",
            "desc": "Purple #9600C8 when following a host.",
            "elements": [
                { "pos": [8, 21], "size": [2, 1], "color": "#9600C8" }
            ]
        },
        {
            "name": "Orchestra Host",
            "desc": "Bright purple #C800FF when hosting.",
            "elements": [
                { "pos": [8, 20], "size": [2, 1], "color": "#C800FF" }
            ]
        }
    ];

    return <MystrixVisualizer
        uiName="Mode Button"
        uiDescription="Columns 8-9: Orchestra sync mode indicator"
        uiElements={uiElements}
    />
}

<ModeButtonDemo />

The **Mode button** (cols 8–9, row 23) is the **Orchestra sync mode indicator** — not a toggle. It reflects the current sync state:

| Color | Meaning |
|-------|---------|
| **Yellow** (#C89600) | Free (not synced to any session) |
| **Purple** (#9600C8) | Follower (connected to a host) |
| **Bright Purple** (#C800FF) | Host (leading a sync session) |
| **Bright Green** (#00E600) | Remote session active |

**See [Song Sections](SongSections.mdx) for section management.**
**See [Multi-Device Sync](../JamWithOthers/MultiDeviceSync.mdx) for Orchestra mode details.**

---

## Sync Button (Cols 10-12)

export function SyncButtonDemo() {
    const uiElements = [
        {
            "name": "No session (idle)",
            "desc": "With no session running the three pixels sit at a dim teal.",
            "elements": [
                { "pos": [10, 23], "size": [3, 1], "color": "#004D66" }
            ]
        },
        {
            "name": "Connected, with peers",
            "desc": "All three pixels go bright cyan once peers are connected.",
            "elements": [
                { "pos": [10, 22], "size": [3, 1], "color": "#00E6E6" }
            ]
        },
        {
            "name": "Hosting",
            "desc": "When you are the host the MIDDLE pixel is overwritten gold. The outer two keep the session colour.",
            "elements": [
                { "pos": [10, 21], "size": [3, 1], "color": "#00E6E6" },
                { "pos": [11, 21], "size": [1, 1], "color": "#FFCC00" }
            ]
        }
    ];

    return <MystrixVisualizer
        uiName="Sync Button States"
        uiDescription="Columns 10-12: Multi-device sync control"
        uiElements={uiElements}
    />
}

<SyncButtonDemo />

### What is Sync?
Multi-device sync allows multiple iOS devices to collaborate in real-time. Devices can share patterns, sync playback, and play together with perfect timing.

### Tap: Open Sync Interface
Opens the sync control panel with options:
- **Start Session**: Become the host
- **Join Session**: See and join available sessions
- **Leave Session**: Disconnect from current session
- **Session Settings**: Configure sync options

### Long-Press: Quick Join/Leave
- **When disconnected**: Quick-join first available session
- **When connected**: Quick-leave current session
- Faster than opening the full menu

### Visual Indicators

| Color | Status | Description |
|-------|--------|-------------|
| **Dim cyan** (#004D66) | Disconnected | No active session |
| **Gold middle pixel** (#FFCC00) | Host | You're controlling the session |
| **Bright cyan** (#00E6E6) | Connected | Peers are in the session |

### Host vs. Follower

#### Host (Gold)
- Controls tempo, key, scale
- Edits master pattern (syncs to all followers in Orchestra mode)
- Manages session settings
- Gold indicator in middle of sync button

#### Follower (Green)
- Receives patterns from host
- Can adjust local volume, mute/solo
- Pattern grid may be locked (Orchestra mode)
- Green indicator shows connected status

**See [Multi-Device Sync](../JamWithOthers/MultiDeviceSync.mdx) for full collaboration guide.**

---

## Device Indicators (Cols 12-35)

export function DeviceIndicatorsDemo() {
    const uiElements = [
        {
            "name": "Connected Devices",
            "desc": "Each device gets a green pixel. Up to 24 devices supported. Indicators only appear when devices are connected.",
            "elements": [
                { "pos": [12, 23], "size": [1, 1], "color": "#00FF00" },
                { "pos": [13, 23], "size": [1, 1], "color": "#00B300" },
                { "pos": [14, 23], "size": [1, 1], "color": "#00B300" },
                { "pos": [15, 23], "size": [1, 1], "color": "#00B300" },
                { "pos": [16, 23], "size": [1, 1], "color": "#00FF00" }
            ]
        }
    ];

    return <MystrixVisualizer
        uiName="Device Indicators"
        uiDescription="Columns 12-35: Shows connected devices (only when active session has participants)"
        uiElements={uiElements}
    />
}

<DeviceIndicatorsDemo />

### What They Show
When devices are connected via WiFi Sync, each device gets a green pixel indicator:
- **Host**: Bright green pixel
- **Follower**: Dimmer green pixel
- **Position**: One pixel per device, starting at column 12

### Maximum Devices
- **Supported**: Up to 24 simultaneous devices
- **Positions**: Columns 12-35 (24 columns)
- **Auto-assignment**: First-come, first-served positioning

### Use Cases
- **See who's connected**: Quick visual roster
- **Session management**: See when devices join/leave

---

## Quick Reference

| Control | Columns | Function | Tap | Long-Press |
|---------|---------|----------|-----|------------|
| **WLED** | 0-3 | LED strip control | Toggle on/off | Settings |
| **Link** | 5-7 | Ableton Link is built into Pixelboop but switched off in the current release, so those three pixels stay dark. | — | — |
| **Mode** | 8-9 | Orchestra sync indicator | — | — |
| **Sync** | 10-12 | Multi-device sync | Sync menu | Quick join/leave |
| **Devices** | 12-35 | Device indicators (when synced) | — | — |

---

**See Also:**
- [Top Controls](TopControls.mdx) - Row 0 (top) controls
- [Multi-Device Sync](../JamWithOthers/MultiDeviceSync.mdx) - Collaboration setup
- [Connectivity](/Connectivity) - MIDI and other connections
