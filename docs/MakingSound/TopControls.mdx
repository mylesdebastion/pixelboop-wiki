---
sidebar_position: 7
---

import PbGridFigure from "@site/src/components/PbGridFigure/PbGridFigure";

# Top Controls

The top bar of the screen contains all playback and musical controls. These buttons let you control tempo, scale, musical key, and more.

## Control Layout Overview

{/* This used to be one 14-zone, 11-label figure covering the whole row --
    an app screenshot never shows it that way; only the wiki tried to. At
    812px (this article's prose column) the worst pair of labels overlapped
    by 59.3px, and even a diagnostic 1093px only got that to 20px: 11 labels
    do not fit one figure at any honest font size. The app's own legend
    only fits ~20 labels because it runs full-screen; a doc column never
    has that room.

    Split into three figures below, but NOT the naive "transport / musical /
    timing" thirds a first pass reaches for -- that split still fails.
    legend.ts's own margin labels are the app's real IntroTutorialStep names
    (LegendOverlayView.swift), not something this page picks, and two of them
    are simply too wide for their own neighbourhood regardless of what else
    shares the figure:
      - "PLAY/PAUSE/STOP" (16 chars) over playStop's 3-column span (cols 0-2)
        collides with "UNDO REDO" 3.5 columns away no matter what other zones
        are focused alongside them -- measured, not assumed: playStop + undo
        + redo alone overlapped by 26.93px at 1440px. Play/Stop and Undo/Redo
        can never share one figure.
      - "SECTION LENGTH" (patternLength) and "SHAKE INDICATOR" (shakeUsb)
        sit only 3.5 columns apart and are BOTH oversized labels; grouped
        together they measured 61.64px overlap -- worse than the original
        composite's 59.3px, because removing the far-away zones did nothing
        for two labels that were already colliding with each other alone.
    Every other adjacency across the whole row is comfortably clear (down to
    scaleType's merged label sitting 7 columns from playStop with room to
    spare), so the fix is routing AROUND those two specific collisions, not
    shrinking text or widening boxes further -- the breakout already in
    PbGridFigure.tsx is at its citation-backed limit for a page this size.

    shakeUsb has no IntroTutorialStep of its own (legend.ts's own documented
    gap) and no natural functional home; it lives here, next to the musical
    controls, only because that is where it is column-distance-safe next to
    ghost (10.5 columns clear) and away from patternLength. Every one of the
    14 original zones still appears in exactly one of the three figures --
    see the coverage check in the PR/commit notes. */}

### Play/Stop and Musical Controls

export function PlayMusicalOverview() {
    return <PbGridFigure
        caption="Play/Stop (columns 0-2), scale quality and type (columns 7-9), the 12-key root note wheel (columns 11-22), the ghost notes toggle (column 24), and the shake indicator (columns 34-35, grouped here for column clearance -- it has no functional relation to the others; see Undo, Redo and Jams below and Timing and MIDI further down for the rest of the row)."
        focus={["playStop", "scaleMajor", "scaleMinor", "scaleType", "rootNoteWheel", "ghost", "shakeUsb"]}
    />
}

<PlayMusicalOverview />

### Undo, Redo, and Jams

export function EditAndJamsOverview() {
    return <PbGridFigure
        caption="Undo (column 4), Redo (column 5), and the Jams button (columns 40 to 43)."
        focus={["undo", "redo", "jams"]}
    />
}

<EditAndJamsOverview />

### Tempo, Pattern Length, and MIDI

export function TimingHardwareOverview() {
    return <PbGridFigure
        caption="Tempo (BPM, columns 26-28), pattern length (columns 30-32), and the USB and Bluetooth MIDI indicators (columns 36-39)."
        focus={["bpm", "patternLength", "usbIndicator", "btIndicator"]}
    />
}

<TimingHardwareOverview />

---

## Play/Stop

export function PlayStopDemo() {
    {/* The old hand-authored version drew this button as ONE cell at col 0.
        The real button (playStop zone, PixelGridUIView.swift:2076) is THREE
        cells, cols 0-2 -- a drift the hand-authored figure had no way to
        catch. The zone-driven figure below draws the real span.
        "Bar Markers" (the old figure's second region, three 1x20 columns at
        cols 4/20/28 inside the step grid) is dropped: it illustrates the
        step grid's own internal bar shading, not a pbgrid zone boundary --
        pbgrid has no zone for it. The bar/beat ruler on the Getting Started
        overview figure already shows this structurally. */}
    return <PbGridFigure
        caption="Play/Stop button, columns 0 to 2. Green when stopped, red when playing. Tap to toggle."
        focus={["playStop"]}
    />
}

<PlayStopDemo />

| State | Color | Description |
|-------|-------|-------------|
| Stopped | 🟢 Green (#45FF45) | Ready to play, tap to start |
| Playing | 🔴 Red (#FF4545) | Sequencer is running, tap to stop |

### Double-Tap: Restart to Step 1

Double-tapping the Play button restarts the pattern from step 1.

- **While playing**: playback keeps going, but jumps back to step 1.
- **While paused**: the playhead resets to step 1 and a **"stop / reset"** tooltip confirms it.
- **In a WiFi jam session**: the restart syncs to every connected player, so the whole session snaps back to step 1 together.

A single tap still toggles play/pause as normal — Pixelboop waits briefly to see if a second tap follows before acting.

### Hold for the Rainbow Menu (Shortcut)

**Hold the Play button for about three and a half seconds** and the **Rainbow menu** opens. A firm haptic tap tells you it caught. This is a shortcut for when your hand is already at the top-left of the grid; the usual way in is to **tap the rainbow gradient in the bottom-left corner** (row 23, cols 0-3). Both gestures land on exactly the same screen.

It is a full screen, not a small dialog with a couple of fields on it. Grid labels, note colors, every light Pixelboop can drive, and the app's own about and licenses all live in there, and the lighting settings (including the address of a WLED strip) sit a level in rather than on the first thing you see. Rather than repeat that layout here, see [The Rainbow Menu](../GettingStarted/RainbowMenu.mdx).

For wiring up LED strips end to end, see the [WLED Setup Guide](../Connectivity/WLED.mdx).

---

## BPM (Tempo)

export function BPMDemo() {
    {/* pbgrid models cols 26-28 as ONE zone ("bpm" / Tempo controls), not
        three. The old figure's separate Down/Display/Up callouts collapse
        into a single region here; the bullet list below still explains all
        three sub-actions in words. */}
    return <PbGridFigure
        caption="Tempo (BPM) controls, columns 26 to 28."
        focus={["bpm"]}
    />
}

<BPMDemo />

- **Default**: 120 BPM
- **Range**: 60–200 BPM
- **Tap**: ±5 BPM
- **Hold**: fine adjustment, repeats ±1 BPM for as long as you hold

---

## Pattern Length

export function PatternLengthDemo() {
    {/* pbgrid models cols 30-32 as ONE zone ("patternLength"), not three --
        same collapse as BPMDemo above. The old figure's fourth region,
        "8-Step Pattern" (a hypothetical cols 4-11 highlight showing what an
        8-step pattern would look like), has no pbgrid zone: it is not a
        fixed UI region, it is an illustration of a DIFFERENT pattern-length
        value than the figure's own default (32). Dropped rather than
        re-invented; the prose below still states the step counts in words. */}
    return <PbGridFigure
        caption="Pattern length controls, columns 30 to 32. The centre readout (col 31) encodes the current length as a hue."
        focus={["patternLength"]}
    />
}

<PatternLengthDemo />

Each tap changes the length by 4 steps, between 8 and 32: **8, 12, 16, 20, 24, 28, 32**. The default is 32. Column 31 shows the current length as a colour that shifts with the value.

---

## Scale Selection

export function ScaleDemo() {
    {/* Clean 1:1 mapping, no drop: three zones, same three columns.
        Intervals (0-2-4-5-7-9-11 etc.) aren't pbgrid data -- they stay in
        the prose/table below, same as before. */}
    return <PbGridFigure
        caption="Scale selection, columns 7 to 9."
        focus={["scaleMajor", "scaleMinor", "scaleType"]}
    />
}

<ScaleDemo />

When you change scales, existing notes **snap to the new scale** — out-of-scale notes move to the nearest in-scale pitch.

| Scale | Notes | Character |
|-------|-------|-----------|
| **Major** | C D E F G A B | Happy, bright |
| **Minor** | C D Eb F G Ab Bb | Sad, emotional |
| **Pentatonic** | C D E G A | Simple, versatile |

---

## Musical Key (Root Note)

export function KeyDemo() {
    {/* The old figure's second region, "Current Key Indicator", was a
        hand-drawn duplicate of what the first region's own colour array
        already showed (col 11 brighter than the rest) -- not a second pbgrid
        zone, just the same fact stated twice. rootNoteWheel's own resolve()
        already brightens the active note (root C by default) and dims the
        other eleven via the app's real dimColor() formula
        (PixelGridUIView+Touch.swift:5794-5797), so one zone says everything
        both old regions said. */}
    return <PbGridFigure
        caption="Root note selector, 12 keys, columns 11 to 22, harmonic (circle-of-fifths) colour wheel."
        focus={["rootNoteWheel"]}
    />
}

<KeyDemo />

| Column | Note | Color |
|--------|------|-------|
| 11 | C | Red |
| 12 | C# | Orange |
| 13 | D | Dark Orange |
| 14 | D# | Yellow-Orange |
| 15 | E | Yellow |
| 16 | F | Yellow-Green |
| 17 | F# | Green |
| 18 | G | Cyan-Green |
| 19 | G# | Cyan |
| 20 | A | Sky Blue |
| 21 | A# | Blue |
| 22 | B | Purple |

Changing the key **transposes all notes** in the pattern to the new key.

---

## Undo, Redo, and Jams

export function EditControlsDemo() {
    {/* Clean 1:1 mapping, no drop. */}
    return <PbGridFigure
        caption="Undo (column 4), Redo (column 5), and Jams (columns 40 to 43)."
        focus={["undo", "redo", "jams"]}
    />
}

<EditControlsDemo />

- **Undo** (col 4): Reverts the last change. Supports multiple levels.
- **Redo** (col 5): Re-applies an undone change.

### The Jams Button (Cols 40-43)

The four purple pixels in the top-right corner are your jam library, not a reset button.

- **Tap**: opens the jam library. Your saved jams are listed with names, dates, and thumbnails, and tapping one loads it back onto the grid.
- **Hold**: saves whatever you have been playing, then clears the grid so you can start something new. A firm haptic tap and a **"NEW JAM"** tooltip confirm it. The hold is short (well under a second), so press and stay put rather than tapping twice.

The purple starts dim and brightens once you have at least one jam saved, so the button tells you at a glance whether the library has anything in it. The figure above shows the dim, empty-library state.

Two things worth knowing before you hold it:

- If you have barely played anything (fewer than five recorded events), there is nothing worth keeping. The grid still clears, but no jam is added to the library.
- If a save genuinely fails, Pixelboop leaves your grid alone. You get a **"Save failed! Jam kept for recovery"** tooltip and your playing stays on screen instead of being wiped.

:::note Coming in the next version
Tapping the Jams button while the library is already open will close it again. Until that ships, each tap opens another copy of the library on top of the last one, and every copy needs its own **Done** tap to clear.
:::

For the whole story on saving, reopening, and starting fresh, see [Saving & Opening Jams](../Jams/SavingAndOpeningJams.mdx).

---

## Ghost Notes Toggle

export function GhostDemo() {
    {/* The old figure's second region, "Ghost Note Example" (three scattered
        dim cells inside the note grid), illustrates a LIVE playback state
        (ghost notes from another section) that pbgrid's zones don't model --
        it is note content, not a fixed UI region. Dropped rather than
        re-invented; the prose below still explains what ghost notes are. */}
    return <PbGridFigure
        caption="Ghost notes toggle, column 24."
        focus={["ghost"]}
    />
}

<GhostDemo />

Ghost notes show faded versions of notes from other song sections, helping you create cohesive arrangements.
